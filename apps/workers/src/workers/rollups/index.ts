// Rollups worker — Phase 0 stub.
// Phase 2 (task #11) lifts: combined metrics tick, metric-bucket-ensure,
// shift-bucket-create, shift-change, archive.

export async function startRollups(): Promise<void> {
  console.log("[rollups] start (Phase 0 stub)");
}

export async function stopRollups(): Promise<void> {
  console.log("[rollups] stop");
}
